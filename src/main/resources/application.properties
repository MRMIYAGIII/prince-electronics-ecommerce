# Server port configuration for Render
server.port=${PORT:8084}

# PostgreSQL database configuration for Render
spring.datasource.url=jdbc:postgresql://princepostgres.dpg-d0on7emcj753dbn2mg-a.frankfurt-postgres.render.com:5432/princepostgres
spring.datasource.driver-class-name=org.postgresql.Driver
spring.datasource.username=${DB_USERNAME}
spring.datasource.password=${DB_PASSWORD}
spring.jpa.hibernate.ddl-auto=update
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect

# Static resources (unchanged)
spring.web.resources.static-locations[0]=file:src/main/resources/static/
spring.web.resources.static-locations[1]=file:./Uploads/
spring.web.resources.static-locations[2]=classpath:/static/
spring.thymeleaf.cache=false

# Mail configuration (unchanged)
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true

# Razorpay configuration (optional, use environment variables if sensitive)
razorpay.keyId=rzp_test_pN1r5PiH89ydFp
razorpay.keySecret=micLOk9wBVrzCJph4pcrYQ7s